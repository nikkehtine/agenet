{
  "workspace": ["./client", "./server"],
  "tasks": {
    "dev": "deno task dev:server & deno task dev:client",
    "dev:client": "deno task --filter='client' dev",
    "dev:server": "deno task --filter='server' dev",
    "start": "deno task --filter='server' start",
    "build": "deno task --filter='client' build && deno compile --allow-net --allow-read --allow-env --include client/dist --output agenet server/main.ts"
  },
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "nodeModulesDir": "auto",
  "unstable": ["fmt-component"]
}
